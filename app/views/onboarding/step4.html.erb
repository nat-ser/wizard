<div class="container-fluid">
  <%= simple_form_for(@session_user, url: create_path) do |f| %>
    <%= f.error_notification %>

    <% if flash.now[:error] %>
      <div class="alert alert-danger"><%= flash[:error] %></div>
    <% end %>

    <div class="form-inputs color-select">
      <%= f.input :fave_color, collection: ["Red", "Orange", "Yellow", "Green", "Blue", "Purple", "Other"], label: "Favorite Color" %>
    </div>

    <div class="form-inputs color-text">
      <% if @view_user.display_prefilled_custom_color_form? %>
          <label class='control-label required' for='user_fave_color'><abbr title='required'>*</abbr> Custom Color</label><input type='text' name='user[fave_color]' value='<%=@session_user.fave_color%>' class='form-control'>
      <% end %>
    </div>

    <%= render "bottom_menu", f: f, step: "step4", submit_text: "Finish", previous_step_path: onboarding_step3_path %>
  <% end %>
</div>

<%= javascript_include_tag "onboarding/step4" %>
